@using Authentication.Models;

@model ClientApp
@{
    ViewBag.Title = "Authorize";
    string displayName = ViewBag.DisplayName;
    string[] scopes = (Request.QueryString.Get("scope") ?? "").Split(' ');
}

<div class="container">
    <h2>Authorize</h2>
    <hr />
    @using (Html.BeginForm("", "", FormMethod.Post))
    {
        <p>Hello @displayName,</p>
        <p>A third party application (@Model.Name) wants to do the following on your behalf:</p>
        <ul>
            @foreach (var scope in scopes)
            {
                <li>@scope</li>
            }
        </ul>
        <p>
            <input type="submit" name="grant" value="Grant" />
            <input type="submit" name="login" value="Sign in as different user" />
        </p>
    }
</div>